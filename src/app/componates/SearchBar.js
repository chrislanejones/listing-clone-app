import React from "react";

const SearchBar = () => {
  return (
    <div className="search-bar">
      <import placeholder="search location" />
      <button>Search</button>
    </div>
  );
};

export default SearchBar;
